{
  "name": "DemoApplication",
  "version": "1.0.0",
  "description": "A simple CAP project.",
  "repository": "<Add your repository here>",
  "license": "UNLICENSED",
  "private": true,
  "dependencies": {
    "@sap/cds": "^7",
    "express": "^4",
    "@sap/cds-hana": "^2"
  },
  "devDependencies": {
    "@cap-js/sqlite": "^1",
    "@sap/cds-dk": "^7"
  },
  "scripts": {
    "build-react": "cd app/my-app && npm install && npm run build",
    "start-watch": "npm run build-react && cds watch",
    "start": "cds run"
  },
  "cds": {
    "requires": {

    }
  }
}
