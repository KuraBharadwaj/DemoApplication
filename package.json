{
  "name": "DemoApplication",
  "version": "1.0.0",
  "description": "A simple CAP project.",
  "repository": "<Add your repository here>",
  "license": "UNLICENSED",
  "private": true,
  "dependencies": {
    "@sap/cds": "^7",
    "express": "^4",
    "@sap/cds-hana": "^2"
  },
  "devDependencies": {
    "@cap-js/sqlite": "^1",
    "@sap/cds-dk": "^7"
  },
  "scripts": {
    "prestart": "cd app/my-app && npm install && npm run build && cp -R dist/* ../app-ui",
    "start": "cds run"
  },
  "cds": {
    "requires": {
      "db": "hana"
    }
  }
}
